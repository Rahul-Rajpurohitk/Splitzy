/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // so Tailwind scans your component files
    ],
    mode: 'jit',
    theme: {
      extend: {},
    },
    plugins: [],
  };
  