spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://postgres:5432/splitzy}
spring.datasource.username=${POSTGRES_USER:splitzy}
spring.datasource.password=${POSTGRES_PASSWORD:splitzy}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Flyway can be enabled later for controlled migrations
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration

